server:
  port: 8083

spring:
  application:
    name: analytics-service

eureka:
  client:
    service-url:
      defaultZone: http://eureka:eureka123@localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    com.inventory.analytics: DEBUG

